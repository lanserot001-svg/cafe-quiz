<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8" />
  <title>무엇을 찾았지?</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    
    .quiz-title {
    font-size: 36px;        /* 글자 크기 키움 */
    font-weight: bold;      /* 진하게 */
    color: black;           /* 글자 색 */
    margin-bottom: 20px;    /* 입력창과의 간격 */
    text-align: center;
    }
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      width: 100vw;
      background-image: url('https://cdn.glitch.global/8ad5a062-999b-4450-bbaa-f3fe6bafa82b/quiz.webp?v=1745590013382');
      
      background-repeat: no-repeat;
      background-position: center center;
      background-size: auto 100vh;
      background-color: #0d0d0d;
      font-family: Arial, sans-serif;
      
      
      /* background-size: cover;
      background-position: center;
      font-family: 'Arial', sans-serif; */
    }

    .canvas-overlay {
      position: absolute;
      top: 42%;
      left: 26.5%;
      width: 48%;
      height: 45%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background-color: rgba(255, 255, 255, 0);
    }

    .canvas-overlay input {
      margin: 5px;
      padding: 10px;
      font-size: 1.1rem;
      width: 80%;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .canvas-overlay button {
      margin-top: 15px;
      padding: 12px 24px;
      font-size: 1.2rem;
      background-color: #ff7f50;
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
    }

    .canvas-overlay button:hover {
      background-color: #ff5722;
    }

    @media (max-width: 768px) {
      .canvas-overlay {
        top: 25%;
        left: 10%;
        width: 80%;
        height: auto;
      }

      .canvas-overlay input,
      .canvas-overlay button {
        width: 100%;
        font-size: 1rem;
      }
    }

    @media (max-width: 480px) {
      .canvas-overlay {
        top: 20%;
        left: 5%;
        width: 90%;
        padding: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="canvas-overlay">
    <h2 class="quiz-title">무엇을 찾았지?</h2>
    <form method="POST" action="/submit">
      <% for (let i = 1; i <= 10; i++) { %>
        <input name="input<%= i %>" placeholder="정답 <%= i %>"><br>
      <% } %>
      <button type="submit">제출하기</button>
    </form>
  </div>
</body>
</html>
